# Tools

## Validate examples

Validate all YAML/JSON files in `examples/` against `schema/pose-contact.schema.json`:

```bash
python tools/validate_examples.py
```

### Requirements

Install dependencies:

```bash
pip install -r requirements.txt
```
